# OpenClaw on NixOS Phase 27 Plan

Phase 27 starts after `openclaw-cloud-consciousness-approved-live-provider-runtime-adapter-module-deferred`. Phases 24-26 defined the runtime adapter module contract, created the approval-gated module task shell, and proved approved module work remains deferred without source mutation.

## Phase 27 Theme

Run a focused regression for the live provider runtime adapter module chain before implementing the first pure adapter function.

## Milestone Slice

1. `openclaw-cloud-consciousness-runtime-adapter-module-regression`
   - Verifies the Phase 24 module contract.
   - Creates the Phase 25 approval-gated runtime adapter module task shell.
   - Approves the task and runs one operator step.
   - Confirms Phase 26 deferred-after-approval evidence.
   - Confirms no source mutation, adapter implementation, SDK load, credential value read, endpoint contact, network egress, or live provider call occurs.

## Exit Boundary

Phase 27 is complete when core and Observer both pass the focused regression for the Phase 24-26 runtime adapter module chain.
